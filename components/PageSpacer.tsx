/**
 * PageSpacer Component
 * 
 * Adds consistent spacing at the top of pages to create appropriate distance between
 * the page content and the navbar/header.
 */

const PageSpacer = () => {
  return (
    <div className="h-32 bg-primary w-full"></div>
  );
};

export default PageSpacer; 